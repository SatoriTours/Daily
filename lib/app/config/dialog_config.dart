/// 对话框配置
class DialogConfig {
  DialogConfig._();

  static const Duration defaultTimeout = Duration(seconds: 30); // 默认超时时间
  static const Duration shortDelay = Duration(milliseconds: 150); // 短延迟时间
  static const Duration mediumDelay = Duration(milliseconds: 300); // 中等延迟时间
}