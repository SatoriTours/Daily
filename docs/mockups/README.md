# Mockup Preview

Use port `8077` for browser mockup previews.

Start from the repository root:

```sh
python3 -m http.server 8077 --bind 0.0.0.0
```

Current LAN preview addresses usually use one of:

- `http://192.168.2.202:8077/`
- `http://192.168.2.108:8077/`
